import { Canvas } from "@react-three/fiber";
import { OrbitControls, Float } from "@react-three/drei";
import { motion } from "framer-motion";
import { Card, CardContent } from "@/components/ui/card";
import { Button } from "@/components/ui/button";

function FloatingDish() {
  return (
    <Float speed={2} rotationIntensity={1.5} floatIntensity={2}>
      <mesh>
        <torusKnotGeometry args={[1, 0.3, 100, 16]} />
        <meshStandardMaterial color="#d4af37" metalness={1} roughness={0.2} />
      </mesh>
    </Float>
  );
}

export default function PremiumMomoCafe() {
  return (
    <div className="min-h-screen bg-black text-white overflow-hidden">
      {/* HERO WITH 3D */}
      <section className="h-screen relative">
        <Canvas camera={{ position: [0, 0, 5] }}>
          <ambientLight intensity={0.5} />
          <directionalLight position={[5, 5, 5]} />
          <FloatingDish />
          <OrbitControls enableZoom={false} />
        </Canvas>

        <div className="absolute inset-0 flex flex-col items-center justify-center text-center">
          <motion.h1
            initial={{ opacity: 0, y: 50 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ duration: 1 }}
            className="text-6xl md:text-8xl font-bold tracking-wide"
          >
            MoMo Café
          </motion.h1>
          <motion.p
            initial={{ opacity: 0 }}
            animate={{ opacity: 1 }}
            transition={{ delay: 0.6 }}
            className="mt-4 text-gray-300 max-w-xl"
          >
            Experience luxury dining with global buffet selections in a refined 3D immersive environment.
          </motion.p>
          <Button className="mt-8 px-8 py-4 text-lg rounded-2xl bg-gradient-to-r from-yellow-500 to-yellow-300 text-black font-semibold">
            Reserve Now
          </Button>
        </div>
      </section>

      {/* FEATURES */}
      <section className="py-24 px-8 grid md:grid-cols-3 gap-10">
        {["Luxury Buffet", "Global Cuisine", "Valet Parking"].map((item, i) => (
          <motion.div
            key={i}
            whileHover={{ scale: 1.08, rotateX: 8, rotateY: 8 }}
            className="perspective"
          >
            <Card className="bg-white/5 backdrop-blur-xl border border-white/10 rounded-2xl shadow-2xl">
              <CardContent className="p-8 text-center">
                <h3 className="text-2xl font-semibold text-yellow-400">
                  {item}
                </h3>
              </CardContent>
            </Card>
          </motion.div>
        ))}
      </section>

      {/* ABOUT */}
      <section className="py-24 px-6 text-center max-w-5xl mx-auto">
        <motion.h2
          initial={{ opacity: 0, y: 30 }}
          whileInView={{ opacity: 1, y: 0 }}
          className="text-5xl font-bold mb-6 text-yellow-400"
        >
          Fine Dining Redefined
        </motion.h2>
        <p className="text-gray-400 text-lg leading-relaxed">
          MoMo Café offers a curated buffet experience blending Western and regional cuisines. Designed with a contemporary aesthetic and immersive 3D visuals, it delivers a premium atmosphere for elite dining.
        </p>
      </section>

      {/* CONTACT */}
      <section className="py-20 text-center border-t border-white/10">
        <h2 className="text-3xl font-bold text-yellow-400">Visit Us</h2>
        <p className="text-gray-400 mt-2">
          Lower Tank Bund Road, Courtyard Hyderabad
        </p>
        <p className="text-gray-500">Open Evenings • Premium Dining</p>
        <Button className="mt-6 bg-yellow-400 text-black px-6 py-3 rounded-xl">
          Call Now
        </Button>
      </section>
    </div>
  );
}
